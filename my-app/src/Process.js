import React from "react";

function Process({nickname, realm}) {
    return (
        <div>
            <h4>Nickname: {nickname}</h4>
            <h4>Realm: {realm}</h4>
        </div>
    )
}

export default Process;